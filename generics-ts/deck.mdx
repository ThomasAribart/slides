import {
  CodeSurfer,
  CodeSurferColumns,
  Step
} from 'code-surfer'
import {
  github,
  vsDark,
  nightOwl
} from '@code-surfer/themes'
import vennDiagrams from './vennDiagrams.png'
import youngExperiment from './Double_fentes.png'
import voyageAuPaysDesMaths from './voyageAuPaysDesMaths.png'
import dynamodb from './dynamodb.jpg'
import moi from './moi.png'
import theodo from './Theodo.png'
import aws from './aws.png'
import ts from './ts.png'
import react from './react.png'
import githubLogo from './github.png'
import discord from './discord.png'
import devTo from './dev-black.png'
import meetup from './meetup.png'
import dynamodbToolshack from './dynamodb-toolshack.png'
import jsonSchemaToTs from './json-schema-to-ts.png'
import castore from './castore.png'
import dynamodbToolbox from './dynamodb-toolbox.png'

export const theme = nightOwl

<img
  src={voyageAuPaysDesMaths}
  style={{
    height: '30%',
    marginBottom: '100px',
    width: 'auto',
    borderRadius: '50px'
  }}
/>

# Voyage au pays des <span style={{ color: "#7FDBCA" }}>génériques</span>

---

<div
  style={{ height: '100vh', width: '100vw', padding: '5%' }}
>
  <h1
    style={{
      height: '10%',
      width: '100%',
      marginTop: 0,
      textAlign: 'center'
    }}
  >
    Thomas Aribart
  </h1>
  <div
    style={{
      height: '85%',
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      gap: '5%'
    }}
  >
    <img
      src={moi}
      width={868}
      height={1216}
      style={{
        flex: 0,
        height: '80%',
        width: 'auto',
        borderRadius: '50px'
      }}
    />
    <div style={{ flex: 1, flexShrink: 0, height: '100%' }}>
      <div
        style={{
          paddingTop: '5%',
          paddingBottom: '5%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'space-between'
        }}
      >
        <div>
          <span>
            TL Full Stack @Theodo{' '}
            <span style={{ fontSize: '1.5rem' }}>
              (2018 > 2024)
            </span>
          </span>
          <div style={{ marginTop: '10px' }}>
            <img
              src={theodo}
              width={796}
              height={172}
              style={{ height: '50px', width: 'auto' }}
            />
          </div>
        </div>
        <div>
          <span>Expert TS, React, Serverless</span>
          <div
            style={{
              marginTop: '10px',
              display: 'flex',
              gap: '20px'
            }}
          >
            <img
              src={ts}
              width={2048}
              height={2048}
              style={{ height: '60px', width: 'auto' }}
            />
            <img
              src={react}
              width={360}
              height={360}
              style={{ height: '60px', width: 'auto' }}
            />
            <img
              src={aws}
              width={1200}
              height={719}
              style={{ height: '60px', width: 'auto' }}
            />
          </div>
        </div>
        <div>
          <span>OpenSourcer</span>
          <div style={{ marginTop: '10px' }}>
            <img
              src={githubLogo}
              width={900}
              height={500}
              style={{ height: '60px', width: 'auto' }}
            />
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

---

<div
  style={{ height: '100vh', width: '100vw', padding: '5%' }}
>
  <h1
    style={{
      height: '10%',
      width: '100%',
      marginTop: 0,
      textAlign: 'center'
    }}
  >
    Thomas Aribart
  </h1>
  <div
    style={{
      height: '85%',
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      gap: '5%'
    }}
  >
    <img
      src={moi}
      width={868}
      height={1216}
      style={{
        flex: 0,
        height: '80%',
        width: 'auto',
        borderRadius: '50px'
      }}
    />
    <div style={{ flex: 1, flexShrink: 0, height: '100%' }}>
      <div
        style={{
          paddingTop: '2%',
          paddingBottom: '2%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'space-between'
        }}
      >
        <span>My libraries:</span>
        <div>
          <div
            style={{
              display: 'flex',
              gap: '40px',
              alignItems: 'center'
            }}
          >
            <img
              src={jsonSchemaToTs}
              style={{
                height: '80px',
                width: 'auto',
                borderRadius: '10px',
                boxShadow: '5px 5px 35px 0 #fff8'
              }}
            />
            <span>json-schema-to-ts</span>
          </div>
          <span
            style={{
              fontSize: '2rem',
              fontWeight: '100',
              marginLeft: '110px'
            }}
          >
            Infer types from JSON Schemas
          </span>
        </div>
        <div>
          <div
            style={{
              display: 'flex',
              gap: '40px',
              alignItems: 'center'
            }}
          >
            <img
              src={castore}
              style={{
                height: '80px',
                width: 'auto',
                borderRadius: '10px',
                boxShadow: '5px 5px 35px 0 #fff8'
              }}
            />
            <span>Castore</span>
          </div>
          <span
            style={{
              fontSize: '2rem',
              fontWeight: '100',
              marginLeft: '110px'
            }}
          >
            Making Event Sourcing easy
          </span>
        </div>
        <div>
          <div
            style={{
              display: 'flex',
              gap: '40px',
              alignItems: 'center'
            }}
          >
            <img
              src={dynamodbToolbox}
              width={3840}
              height={2160}
              style={{
                height: '80px',
                width: 'auto',
                borderRadius: '10px',
                boxShadow: '5px 5px 35px 0 #fff8'
              }}
            />
            <span>DynamoDB-Toolbox</span>
          </div>
          <span
            style={{
              fontSize: '2rem',
              fontWeight: '100',
              marginLeft: '110px'
            }}
          >
            Lightweight and type-safe query builder for
            DynamoDB
          </span>
        </div>
      </div>
    </div>
  </div>
</div>

---

<h3>
  <u>https://dynamodb-toolshack.com</u>
</h3>

<img
  src={dynamodbToolshack}
  width={2048}
  height={2048}
  style={{
    height: '70%',
    width: 'auto',
    borderRadius: '5px',
    boxShadow: '0 35px 60px -15px white'
  }}
/>

---

#1

# 🤔 Un type <span style={{ color: "#7FDBCA" }}>générique</span> ?

---

<CodeSurfer>

```ts
type Contained = { value: unknown }
```

```ts
type Contained = { value: unknown }

type Contained<VALUE> = { value: VALUE }
```

```ts
type Contained = { value: unknown }

type Contained<VALUE> = { value: VALUE }

type Str = Contained<string> // { value: string }
type Num = Contained<number> // { value: number }
```

<!-- prettier-ignore -->
```ts
type Contained<
  VALUE extends string | number = "default"
> = { value: VALUE }

type Str = Contained<string> // { value: string }
type Num = Contained<number> // { value: number }
```

</CodeSurfer>

<Notes>

A tout type on peut associer un type générique, que j'ai appelé VALUE. On le place entre chevrons comme ça.

Ca nous permet de ré-utiliser ce type dans des contextes différents.

</Notes>

---

## Pourquoi utiliser des génériques ?

### 🤫 DRY

<h3 style={{ opacity: 0.5 }}>?</h3>

<h3 style={{ opacity: 0.5 }}>?</h3>

---

#1 (bis)

# 🤔 Des <span style={{ color: "#7FDBCA" }}>fonctions</span> génériques ?

---

<CodeSurfer>

```ts
const contain = (value: unknown) => ({ value })
```

```ts
const contain = (value: unknown) => ({ value })

contain('foo') // { value: unknown }
```

```ts
const contain = <VALUE>(
  value: VALUE
): Contained<VALUE> => ({ value })

contain('foo') // { value: unknown }
```

```diff 1[17:24],2,3[1:2]

```

```diff 3[4:19]

```

```ts
const contain = <VALUE>(
  value: VALUE
): Contained<VALUE> => ({ value })

contain<'foo'>('foo')
// { value: 'foo' }
```

```diff 5[1:7,15:21],6

```

```ts 5,6
const contain = <VALUE>(
  value: VALUE
): Contained<VALUE> => ({ value })

contain('foo')
// { value: 'foo' }
```

</CodeSurfer>

<Notes>

J'ai écrit ici une fonction toute simple qui prend une valeur et la met dans un objet.

Le type générique va venir s'appliquer aux argument, et peut être utilisé pour typer le retour de la fonction.

On obtient cette syntaxe, qui est un peu redondante. La beauté, c'est que TypeScript peut inférer la valeur du générique à partir de l'argument.

</Notes>

---

#1 (bis²)

# 🤔 Des <span style={{ color: "#7FDBCA" }}>classes</span> génériques ?

---

<CodeSurfer>

```ts
class Container<KEY extends string> {
  key: KEY

  constructor(key: KEY) {
    this.key = key
  }

  contain<VALUE>(value: VALUE): { [K in KEY]: VALUE } {
    return { [this.key]: value }
  }
}
```

```diff 1[16:35],2:6

```

```diff 8[10:53]

```

```ts
class Container<KEY extends string> {
  key: KEY

  constructor(key: KEY) {
    this.key = key
  }

  contain<VALUE>(value: VALUE): { [K in KEY]: VALUE } {
    return { [this.key]: value }
  }
}

const container = new Container('value')
container.contain('foo') // { value: 'foo' }
```

</CodeSurfer>

<Notes>

Encore une fois, une fois la classe écrite, on n'a plus besoin d'écrire de type générique.

</Notes>

---

## Pourquoi utiliser des génériques ?

### 🤫 DRY

### 🌈 Améliore grandement le type-system

<h3 style={{ opacity: 0.5 }}>?</h3>

---

## Pourquoi utiliser des génériques ?

### 🤫 DRY

### 🌈 Améliore grandement le type-system

### 🙈 ...sans même que vous ayez besoin de le savoir

---

<CodeSurfer>

```ts
import { z } from 'zod'

const schema = z.literal('foo')
```

```ts 3,4
import { z } from 'zod'

const schema = z.literal('foo')
// z.ZodLiteral<'foo'>
```

```ts
import { z } from 'zod'
import { zodResolver } from '@hookform/resolvers/zod'
import { useForm } from 'react-hook-form'

const schema = z.literal('foo')
// z.ZodLiteral<'foo'>

const MyForm = () => {
  const form = useForm({
    resolver: zodResolver(schema)
  })
  // UseFormReturn<'foo'>

  return <Form {...form}>...</Form>
}
```

```ts 5[25:31],9[23:29],14[15:21]
import { z } from 'zod'
import { zodResolver } from '@hookform/resolvers/zod'
import { useForm } from 'react-hook-form'

const schema = z.literal<'foo'>('foo')
// z.ZodLiteral<'foo'>

const MyForm = () => {
  const form = useForm<'foo'>({
    resolver: zodResolver(schema)
  })
  // UseFormReturn<'foo'>

  return <Form<'foo'> {...form}>...</Form>
}
```

</CodeSurfer>

<Notes>

Il est très probable que vous utilisiez déjà des génériques dans votre code base

Ce qu'il se passe c'est qu'il y a des génériques qui opèrent sous le capot

</Notes>

---

#2

# 🤔 <span style={{ color: "#7FDBCA" }}>Transformer</span> des génériques ?

<i style={{ opacity: 0 }}>
  (*surtout utile pour les librairies)
</i>

<Notes>

Définir des génériques, c'est pas la seule chose qu'on faire: on va voir qu'on peut aussi les transformer au passage.

</Notes>

---

#2

# 🤔 <span style={{ color: "#7FDBCA" }}>Transformer</span> des génériques ?

<i>(*surtout utile pour les librairies)</i>

<Notes>

Définir des génériques, c'est pas la seule chose qu'on faire: on va voir qu'on peut aussi les transformer au passage.

</Notes>

---

<img src={dynamodb} />

### Un exemple concret: Le <span style={{ color: "#7FDBCA" }}>marshalling</span>

---

<CodeSurfer>

```ts
const marshall<VALUE>: Marshall<Value> => { ... }
```

```ts
const marshall<VALUE>: Marshall<Value> => { ... }

marshall('foo') // { S: 'foo' }
marshall(42) // { N: 42 }
```

```ts
const marshall<VALUE>: Marshall<Value> => { ... }

marshall('foo') // { S: 'foo' }
marshall(42) // { N: 42 }

type Marshall<VALUE> = ???
```

</CodeSurfer>

<Notes>

L'exercice est le suivant: Typer une fonction Marshall qui opère

</Notes>

---

# Let's code 🤓

---

### Le problème avec <span style={{ color: '#7FDBCA' }}>`extends`</span>

<CodeSurferColumns>

<Step>

```ts
class A extends B {
  extension: string
}
```

```ts
class B {
  base: string
}
```

</Step>

<Step>

```ts 1:4
class A {
  base: string
  extension: string
}
```

```ts
class B {
  base: string
}
```

</Step>

</CodeSurferColumns>

<Notes>

Viens du monde des classes

On voit l'extension visuellement: On agrandit quelque chose

</Notes>

---

### Le problème avec <span style={{ color: '#7FDBCA' }}>`extends`</span>

### Laquelle de ces assertions est vraie ?

<CodeSurfer>

```ts
// A
type AssertA = string | number extends string ? true : false

// B
type AssertB = string extends string | number ? true : false
```

```diff 4:5

```

</CodeSurfer>

---

### Le problème avec <span style={{ color: '#7FDBCA' }}>`extends`</span>

<CodeSurferColumns>

<Step>

```ts
A extends B
 ⇔ A has B properties and more
```

<img
  src={vennDiagrams}
  style={{ opacity: 0, height: '50%', width: 'auto' }}
/>

</Step>

<Step>

```ts
A extends B
 ⇔ A has B properties and more
 ⇔ A stricter than B
```

<img
  src={vennDiagrams}
  style={{ opacity: 0, height: '50%', width: 'auto' }}
/>

</Step>

<Step>

```ts
A extends B
 ⇔ A has B properties and more
 ⇔ A stricter than B
 ⇔ A ⊆ B
```

<img
  src={vennDiagrams}
  style={{ height: '50%', width: 'auto' }}
/>

</Step>

<Step>

```ts
A extends B
 ⇔ A has B properties and more
 ⇔ A stricter than B
 ⇔ A ⊆ B

✅ string extends string | number
```

<img
  src={vennDiagrams}
  style={{ height: '50%', width: 'auto' }}
/>

</Step>

</CodeSurferColumns>

---

## ☝️ Utiliser <span style={{ color: '#7FDBCA' }}>"strict"</span> ou <span style={{ color: '#7FDBCA' }}>"loose"</span> pour qualifier vos types

<p>
  Et pas:{' '}
  <span style="color:red;text-decoration:line-through">
    <span style="color:#d6deeb">Big</span>
  </span>
  , <span style="color:red;text-decoration:line-through">
    <span style="color:#d6deeb">large</span>
  </span>, <span style="color:red;text-decoration:line-through">
    <span style="color:#d6deeb">small</span>
  </span>, <span style="color:red;text-decoration:line-through">
    <span style="color:#d6deeb">narrow</span>
  </span>...
</p>

<Notes>

Conseil: Bannir les adjectif de taille

</Notes>

---

# Est-ce qu'on a fini? 🤓

---

<CodeSurfer>

```ts 1:3
type VALUE extends string | number
// string
// number
```

```ts 1:5
type VALUE extends string | number
// string
// number
// string | number
// never
```

</CodeSurfer>

<Notes>

Est-ce qu'on a couvert tous les edge cases possible?

Non il y en a une infinité, mais il y en a deux en particuliers qui m'intéressent: Lesquels?

</Notes>

---

### Que vaut <span style={{ color: '#7FDBCA' }}><code>Marshall&lt;string | number&gt;</code></span>?

<CodeSurfer>

```ts
type Marshall<VALUE> = VALUE extends string
  ? { S: VALUE }
  : VALUE extends number
  ? { N: VALUE }
  : never
```

```ts
type Marshall<VALUE> = VALUE extends string
  ? { S: VALUE }
  : VALUE extends number
  ? { N: VALUE }
  : never

// A
type TestA = Marshall<string | number> // never

// B
type TestB = Marshall<string | number> // { S: string } | { N: number }
```

```diff 10:11

```

</CodeSurfer>

---

### <span style={{ color: '#7FDBCA' }}><code>Extends</code></span> est un peu <span style={{ color: '#7FDBCA' }}>quantique</span>

<img
  src={youngExperiment}
  style={{ background: 'blanchedalmond', padding: '100px' }}
/>

### Par <span style={{ color: '#7FDBCA' }}>quelle fente</span> passe l'électron?

<Notes>

J'aime bien dire que extends un peu quantique. Dans l'expérience des fentes de Young, on lance un électron sur un écran opaque avec deux fentes, et on pose la question: Par quelle fente est-il passé?
La réponse est: Les deux

- Le cas double est déjà géré car `string` et `number` se retrouvent
- Le cas `never` est déjà géré car l’éclatement ne génère aucun élément

</Notes>

---

### <span style={{ color: '#7FDBCA' }}><code>Extends</code></span> est un peu <span style={{ color: '#7FDBCA' }}>quantique</span>

<img
  src={youngExperiment}
  style={{ background: 'blanchedalmond', padding: '100px' }}
/>

### Les <span style={{ color: '#7FDBCA' }}>deux fentes</span>!

<Notes>

J'aime bien dire que extends un peu quantique. Dans l'expérience des fentes de Young, on lance un électron sur un écran opaque avec deux fentes, et on pose la question: Par quelle fente est-il passé?
La réponse est: Les deux

- Le cas double est déjà géré car `string` et `number` se retrouvent
- Le cas `never` est déjà géré car l’éclatement ne génère aucun élément

</Notes>

---

<CodeSurfer>

```ts
(
  | string
  | number
) extends string
  ? ...
  : ...
```

```ts 1:11
(
  | string
) extends string
  ? ...
  : ...

(
  | number
) extends string
  ? ...
  : ...
```

```ts 1:7
Marshall<string>

(
  | number
) extends string
  ? ...
  : ...
```

<!-- prettier-ignore -->
```ts 1:3
Marshall<string>
| Marshall<number>
// { S: string } | { N: number }
```

</CodeSurfer>

<Notes>

Never mais seulement parce que la distributivité renvoie l'ensemble vide pas parce qu'il y a never ici

Je pourrais renvoyer toto que ça serait la même

</Notes>

---

### Que vaut <span style={{ color: '#7FDBCA' }}><code>Marshall&lt;never&gt;</code></span>?

<CodeSurfer>

```ts
type Marshall<VALUE> = VALUE extends string
  ? { S: VALUE }
  : VALUE extends number
  ? { N: VALUE }
  : never
```

```ts
type Marshall<VALUE> = VALUE extends string
  ? { S: VALUE }
  : VALUE extends number
  ? { N: VALUE }
  : never

// A
type TestA = Marshall<never> // never

// B
type TestB = Marshall<never> // { S: never } | { N: never }
```

```ts 7:9
type Marshall<VALUE> = VALUE extends string
  ? { S: VALUE }
  : VALUE extends number
  ? { N: VALUE }
  : never

// A
type TestA = Marshall<never> // never
// ...car la distributivité renvoie ∅ !

// B
type TestB = Marshall<never> // { S: never } | { N: never }
```

</CodeSurfer>

---

### Pour <span style={{ color: '#7FDBCA' }}>opt out</span> de la distributivé:

<CodeSurfer>

```ts
// Distribué
type Type<VALUE> = VALUE extends A ? B : C

// 🙅‍♂️ Non-distribué
type Type<VALUE> = [VALUE] extends [A] ? B : C
type Type<VALUE> = { k: VALUE } extends { k: A } ? B : C
```

</CodeSurfer>

<Notes>

Il y a donc une différence entre `{ key: value } extends { key: string }` et `value extends string` !

</Notes>

---

# Oui! On a fini 🎉

---

#3

# Aller plus loin: La <span style={{ color: "#7FDBCA" }}>récursion</span> 😵‍💫

---

<CodeSurfer>

```ts
const marshall<VALUE>: Marshall<Value> => { ... }

marshall('foo') // { S: 'foo' }
marshall(42) // { N: 42 }
```

```ts 1,6:10
const marshall<VALUE>: Marshall<Value> => { ... }

marshall('foo') // { S: 'foo' }
marshall(42) // { N: 42 }

marshall({ foo: 42 })
// { M: { foo: Marshall<42> } }
marshall(['foo', 42])
// { L: [Marshall<'foo'>, Marshall<42>] }
```

</CodeSurfer>

<Notes>

Pour l'instant on a codé marshall sur des valeurs littérales. Le marshalling est récursif.

</Notes>

---

# Let's code 🤓

---

<div
  style={{ height: '100vh', width: '100vw', padding: '5%' }}
>
  <h1
    style={{
      height: '10%',
      width: '100%',
      marginTop: 0,
      textAlign: 'center'
    }}
  >
    Merci ❤️
  </h1>
  <div
    style={{
      height: '85%',
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      gap: '5%'
    }}
  >
    <img
      src={moi}
      width={868}
      height={1216}
      style={{
        flex: 0,
        height: '80%',
        width: 'auto',
        borderRadius: '50px'
      }}
    />
    <div style={{ flex: 1, flexShrink: 0, height: '100%' }}>
      <div
        style={{
          paddingTop: '10%',
          paddingBottom: '10%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'space-between'
        }}
      >
        <div style={{ display: 'flex', gap: '40px' }}>
          <img
            src={githubLogo}
            width={900}
            height={500}
            style={{ height: '60px', width: 'auto' }}
          />
          <span>https://github.com/ThomasAribart</span>
        </div>
        <div style={{ display: 'flex', gap: '40px' }}>
          <img
            src={devTo}
            width={1998}
            height={1998}
            style={{ height: '60px', width: 'auto' }}
          />
          <span>https://dev.to/thomasaribart</span>
        </div>
        <div style={{ display: 'flex', gap: '40px' }}>
          <img
            src={meetup}
            width={3840}
            height={2160}
            style={{ height: '60px', width: 'auto' }}
          />
          <img
            src={discord}
            width={1818}
            height={2049}
            style={{ height: '60px', width: 'auto' }}
          />
          <span>Paris TS meetup</span>
        </div>
      </div>
    </div>
  </div>
</div>

---

#4

# Quelques <span style={{ color: "#7FDBCA" }}>warnings</span> ⚠️

---

### TS <span style={{ color: "#7FDBCA" }}>ne type-checke pas</span> (encore) le JS d'un type générique

<CodeSurfer>

```ts
const marshall = <VALUE extends string | number>(
  value: VALUE
): Marshall<VALUE> => {
  if (typeof value === 'string') {
    return { S: value } // ❌ Fails
  }

  return ...
}
```

</CodeSurfer>

---

### Attention à la <span style={{ color: "#7FDBCA" }}>contravariance</span>

<CodeSurfer>

```ts
type f = (...args: ParamsF) => ReturnF
type g = (...args: ParamsG) => ReturnG

f extends g only if...
```

```ts 1,2,5
type f = (...args: ParamsF) => ReturnF
type g = (...args: ParamsG) => ReturnG

f extends g only if...
    ...ReturnF ⊆ ReturnG // covariance
```

```ts 1,2,5,6
type f = (...args: ParamsF) => ReturnF
type g = (...args: ParamsG) => ReturnG

f extends g only if...
    ...ReturnF ⊆ ReturnG // covariance
...AND ParamsF ⊇ ParamsG // contravariance
```

</CodeSurfer>

---

### On ne peut pas <span style={{ color: "#7FDBCA" }}>génériser</span> des génériques

<CodeSurfer>

```ts
const prefix = (string) => `_${string}`
const prefixMap = (strings) => strings.map(prefix)
```

```ts
const prefix = (string) => `_${string}`
const prefixMap = (strings) => strings.map(prefix)

type Prefix<STR> = `_${STR}`
// ❌ Not possible
type PrefixMap<STRINGS> = Map<STRINGS, Prefix>
```

```ts
const prefix = (string) => `_${string}`
const prefixMap = (strings) => strings.map(prefix)

import { Call, S, T } from 'hotscript'
type Prefix = S.Prepend<'_'>
// ✅ Possible but next level 🌶️
type PrefixMap<STRINGS> = Call<T.Map<Prefix>, STRINGS>
```

</CodeSurfer>
